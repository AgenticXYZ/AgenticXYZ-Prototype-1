# User Overlay boundary

This directory is intentionally empty in the public repository. A User-side Agent may create a reversible local `user-overlay.json` after Contributor approval, but private user realization state is never committed or automatically copied into a KPR.
